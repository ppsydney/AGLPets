namespace AGL.AGLPets.Utilities
{
    public static class AGLPetsEnums
    {
        // TODO: Consider retrieving this list from the AGLPets webservice and caching it, not in the FS though
        public enum PetTypeEnum 
        {
            Cat,
            Dog,
            Fish,
            Elephant
        }
    }
}
